# Six Threads

Upload **all the files in this folder** to the `six-threads` repository on GitHub,
keeping their names exactly as they are.

- `index.html` is the app
- `manifest.webmanifest`, `sw.js` and the three icons make it installable on a phone

None of these files contain any of your data.
